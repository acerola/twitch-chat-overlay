# Default Twitch channel used when URL does not include ?channel=
VITE_CHANNEL_NAME=your_channel_name

# Enable debug menu and debug fixtures by default:
# 0 = disabled, 1 = enabled
VITE_DEBUG_MODE=1

# Twitch application client ID (Public app type)
# Register at https://dev.twitch.tv/console
# No redirect URIs needed — uses Device Code Grant Flow
VITE_TWITCH_CLIENT_ID=

# EventSub WebSocket URL override (for local testing with Twitch CLI mock server)
# Default: wss://eventsub.wss.twitch.tv/ws
# Start mock server: twitch event websocket start-server
# Then set: ws://127.0.0.1:8080/ws
VITE_EVENTSUB_URL=
